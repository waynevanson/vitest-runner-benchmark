# Integration Testing

We run Vitest with some preconfigured and parameterised values.

The parameters that files matching `test/*.spec.ts` launch Vitest tests from one of the dirs which is a parameter.

We also have some custom matchers for testing against the vitest instance.
